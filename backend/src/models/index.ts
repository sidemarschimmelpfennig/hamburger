import { Sequelize } from 'sequelize';
import sequelize from '../database/connection';

// Importe e registre seus models aqui
// import User from './User';

// Associações de models ficam aqui
// User.hasMany(Order);

export { sequelize, Sequelize };
